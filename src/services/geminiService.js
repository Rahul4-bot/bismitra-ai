/**
 * DEPRECATED / MOVED SERVER-SIDE
 * All Gemini AI grounding and API interactions have been securely relocated
 * to the serverless endpoint at api/chat.js to prevent exposing API keys
 * to the browser bundle.
 */
export default {};
