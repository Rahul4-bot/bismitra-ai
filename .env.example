# BISMITRA AI - Server-Side Environment Configuration
# Add your Google Gemini API key below.
# Stored securely on the server-side only (never exposed in client bundle).
# On Vercel, configure this variable in Project Settings -> Environment Variables.

GEMINI_API_KEY=
